pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "✅ Code has been checked out"
            }
        }

        stage('Test') {
            steps {
                echo "✅ Jenkins is running the Test stage"
            }
        }
    }
}

